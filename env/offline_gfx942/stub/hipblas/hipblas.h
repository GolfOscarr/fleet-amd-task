/* Stub of hipBLAS for the host syntax check of the patched runtime sources
 * (env/offline_gfx942/run.sh, the host step); the fork's headers include it
 * but use nothing from it on the paths the check parses. Nothing here runs.
 */
#pragma once
