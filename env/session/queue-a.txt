# Session A, part 1: the fault (docs/round-2/02-session-plan.md, row A5).
# Rows run one at a time; 'continue' keeps the queue going after an expected FAIL.
--layers 8 --head --iters 2 continue                 # A5.1: the fault reproduces on this machine (expected FAIL)
--layers 8 --head --iters 2 --pad-alloc 1 continue   # A5.2: does the address decide? any PASS here says yes
--layers 8 --head --iters 2 --pad-alloc 2 continue
--layers 8 --head --iters 2 --pad-alloc 4 continue
--layers 16 --head --iters 2 --pad-alloc 1 continue  # A5.2: the converse: a 16-layer graph that ran, shifted
# A6 is the bisection, not a queue row:
#   bash env/session/vm.sh start bisect env/session/queue-fault.txt -- --layers 8 --head --iters 2
# A7 is queue-fix.txt (the pre-baked fixes); then queue-a2.txt (A8).
