# Session A, part 2: after the fix (row A8 of docs/round-2/02-session-plan.md).
--layers 8 --head --iters 2                          # A8.1: the fixed 8-layer graph runs (add the winning flag of A7 by hand)
--layers 27 --head --iters 32 compare                # A8.2: M4, 32 ids against harness/ref/ref_output_ids.json
--layers 27 --debug compare                          # A8.3: the growth curve over 27 layers (needs P3)
# A8.4 (B0): the attribution check; the two runs differ by one plain gang linear (docs/round-2/02-session-plan.md)
--layers 2 --stop-after L0.norm1 --iters 4 measure continue
--layers 2 --stop-after L0.qkva --iters 4 measure continue
--layers 2 --iters 32 --event-timing table           # A8.5: the 2-layer timing, gang linears, P6 kernels
--layers 2 --iters 32 --event-timing --tile-linears table         # A8.5: the same with per-tile linears (P5)
--layers 27 --head --iters 32 --event-timing table   # A8.6: the 27-layer baseline of this machine (15.6 ms on 2026-09-15)
