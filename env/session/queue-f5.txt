# Round 4 finals, G7 and H6 (docs/gpu-experiments/04-kernels/02-local-gpu-split.md, 04-router-merge-split.md):
# every lever that passed, three times at 30, 31 and 32 iterations (distinct run names, the record keeps
# all three), then once at 29 without the event timing for the megakernel's own FWD_PASS clock. Written
# with the whole stack (the GEMV linears, w13 in one round, the router in four tasks, the merge with
# o_proj folded in); a lever that failed its row comes out with env/session/queue_flag.py before this
# file runs (round 3's mid-session edit). Runs after queue-g2.txt.
--layers 27 --head --iters 30 --tile-linears --nt-weights --event-timing --fuse-norm2 --fuse-silu --fuse-norm1 --mfma-attend --attend-tasks --nt-streams --gemv-linears --runtime-flags=-DMPK_W2_CK_TILE --merge-tasks --merge-halves 2 table continue
--layers 27 --head --iters 31 --tile-linears --nt-weights --event-timing --fuse-norm2 --fuse-silu --fuse-norm1 --mfma-attend --attend-tasks --nt-streams --gemv-linears --runtime-flags=-DMPK_W2_CK_TILE --merge-tasks --merge-halves 2 table continue
--layers 27 --head --iters 32 --tile-linears --nt-weights --event-timing --fuse-norm2 --fuse-silu --fuse-norm1 --mfma-attend --attend-tasks --nt-streams --gemv-linears --runtime-flags=-DMPK_W2_CK_TILE --merge-tasks --merge-halves 2 table compare continue
--layers 27 --head --iters 29 --tile-linears --nt-weights --fuse-norm2 --fuse-silu --fuse-norm1 --mfma-attend --attend-tasks --nt-streams --gemv-linears --runtime-flags=-DMPK_W2_CK_TILE --merge-tasks --merge-halves 2 table compare continue    # FWD_PASS, no event timing
