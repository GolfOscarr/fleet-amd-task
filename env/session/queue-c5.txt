# Session C, row G5 (docs/gpu-experiments/03-acceleration/05-session-plan.md): the three fusions, added one at a time
# (O1 the router norm, O2 the silu into w2, O3 the norm into the per-tile linear), each on the 2-layer graph with the
# boundary compare, then on the whole model with the ids. A lever stays on if the compare passes and the event clock
# improves; the next row assumes the previous lever passed (the queue continues either way).
--layers 2 --iters 32 --tile-linears --nt-weights --event-timing --worker-timing --fuse-norm2 table compare continue
--layers 27 --head --iters 32 --tile-linears --nt-weights --event-timing --worker-timing --fuse-norm2 table compare continue
--layers 2 --iters 32 --tile-linears --nt-weights --event-timing --worker-timing --fuse-norm2 --fuse-silu table compare continue
--layers 27 --head --iters 32 --tile-linears --nt-weights --event-timing --worker-timing --fuse-norm2 --fuse-silu table compare continue
--layers 2 --iters 32 --tile-linears --nt-weights --event-timing --worker-timing --fuse-norm2 --fuse-silu --fuse-norm1 table compare continue
--layers 27 --head --iters 32 --tile-linears --nt-weights --event-timing --worker-timing --fuse-norm2 --fuse-silu --fuse-norm1 table compare continue
