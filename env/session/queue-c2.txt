# Session C, row G2 (docs/gpu-experiments/03-acceleration/05-session-plan.md): the round-2 baseline with the
# worker timing (I1) beside the event clock: exec cycles per class next to the event gaps. Both graphs of round 2,
# the flags of its best steady state (per-tile linears, non-temporal weight loads). The spin standalone and in a
# graph is the ktime stage and the ladder's --spin rows (queue-c3.txt).
--layers 2 --iters 32 --tile-linears --nt-weights --event-timing --worker-timing table compare continue                # G2.1: the 2-layer timing, 16 boundaries compared
--layers 27 --head --iters 32 --tile-linears --nt-weights --event-timing --worker-timing table compare continue        # G2.2: the 27-layer baseline of this machine, the 32 ids
