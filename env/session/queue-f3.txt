# Round 4, G3 and G4 (docs/gpu-experiments/04-kernels/02-local-gpu-split.md): the w2 GEMV form, which is
# the pushed header's default path of --fuse-silu (its CK fallback is the -DMPK_W2_CK_TILE define of
# queue-f2.txt's G1 rows, the A/B), then w13 in one round per XCD (--gemv-w13, 37 tiles per expert). Each
# as the pair on the 2-layer graph with the GEMV linears on; the w2 gap against 23.9 us, w13's against 42.5.
--layers 2 --iters 1 --tile-linears --nt-weights --event-timing --fuse-norm2 --fuse-silu --fuse-norm1 --mfma-attend --attend-tasks --nt-streams --gemv-linears compare continue    # G3: w2 as a GEMV
--layers 2 --iters 32 --tile-linears --nt-weights --event-timing --worker-timing --fuse-norm2 --fuse-silu --fuse-norm1 --mfma-attend --attend-tasks --nt-streams --gemv-linears table continue    # G3
--layers 2 --iters 1 --tile-linears --nt-weights --event-timing --fuse-norm2 --fuse-silu --fuse-norm1 --mfma-attend --attend-tasks --nt-streams --gemv-linears --gemv-w13 compare continue    # G4: w13 in one round
--layers 2 --iters 32 --tile-linears --nt-weights --event-timing --worker-timing --fuse-norm2 --fuse-silu --fuse-norm1 --mfma-attend --attend-tasks --nt-streams --gemv-linears --gemv-w13 table continue    # G4; H1 and H3 read the router and merge exec from this row
